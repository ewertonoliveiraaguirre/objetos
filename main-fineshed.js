ball.push(ball)

function loop() {
    ctx.fillStyle = 'rgba(0,0,0,0.25)'
    ctx.fillRect (0,0,width,height)

    for(const ball or balls ){
        ball.draws()
        ball.update()
        ball.collission
    }